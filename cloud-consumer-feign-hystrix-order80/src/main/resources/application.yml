server:
  port: 80

eureka:
  client:
  #false的话，则不会去服务中心拉取服务，导致找不到服务端
    fetch-registry: true
    service-url:
      defaultZone: http://eureka7001.com:7001/eureka/
feign:
  hystrix:
    enabled: true